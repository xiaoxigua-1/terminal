export interface Project {
  name: string;
  // eslint-disable-next-line camelcase
  full_name: string;
  // eslint-disable-next-line camelcase
  html_url: string;
  // eslint-disable-next-line camelcase
  stargazers_count: number;
  // eslint-disable-next-line camelcase
  forks_count: number;
  fork: boolean;
  // eslint-disable-next-line camelcase
  forks_url: string;
}
